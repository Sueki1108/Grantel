{
  "hosting": {
    "public": ".next",
    "rewrites": [
      {
        "source": "**",
        "function": "server"
      }
    ],
    "predeploy": [
      "npm --prefix . run build",
      "npm --prefix functions install"
    ]
  },
  "functions": {
    "source": "functions"
  }
}
