rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow access for any signed-in user.
      // This includes anonymous authentication.
      allow read, write: if request.auth != null;
    }
  }
}